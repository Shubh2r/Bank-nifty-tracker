name: Run BankNifty Bot

on:
  workflow_dispatch:  # Allows manual trigger from GitHub UI

permissions:
  contents: write
  contents: write  # ✅ Enables push access

jobs:
  run-bot:
@@ -22,20 +22,18 @@
      - name: 📦 Install dependencies
        run: pip install -r requirements.txt

      - name: 🚀 Run bot script
        run: python bot.py
      - name: 🚀 Run bot script and generate report
        run: |
          echo "# Report - $(date)" > report.md
          python bot.py >> report.md

      - name: 💾 Commit report to reports/ folder
        run: |
          git config --global user.name "GitHub Bot"
          git config --global user.email "bot@example.com"

          mkdir -p reports
          if [ -f report.md ]; then
            mv report.md reports/report.md
          elif [ ! -f reports/report.md ]; then
            echo "# Report" > reports/report.md
          fi
          mv report.md reports/report.md

          git add reports/report.md
          git commit -m "Add or update report in reports folder" || echo "No changes to commit"
